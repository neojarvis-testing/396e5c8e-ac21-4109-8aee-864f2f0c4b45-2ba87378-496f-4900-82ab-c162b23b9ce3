import { CropSearchPipe } from './crop-search.pipe';

describe('CropSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CropSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
